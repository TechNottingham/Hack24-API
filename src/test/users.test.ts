import * as assert from 'assert';

describe('Users resource', () => {
  
  describe('Get all users', () => {
    
    before(() => {
      
    });
    
    it('should pass', () => {
      assert.ok(true);
    });
    
  });
  
});